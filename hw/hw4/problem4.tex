\begin{problem}
  % define compactness
  % \begin{definition}
  %   A set $S$ is compact if every open cover of $S$ has a finite subcover.
  % \end{definition}

  % % define open cover
  % \begin{definition}
  %   An open cover of a set $S$ is a collection of open sets $\set{U_\alpha}_{\alpha \in A}$ such that $S \subseteq \bigcup_{\alpha \in A}{U_\alpha}$.
  % \end{definition}

  Let $\displaystyle A \colonequals \set{\frac{1}{n} \given n \in \N} \subset \R$.
  \begin{enumroman}
    \item Show that $A$ is not compact directly using the definition.
      \begin{answer}
        
      \end{answer}
    \item Show that $A \cup \set{0}$ is compact directly using the definition.
      \begin{answer}
        
      \end{answer}
  \end{enumroman}
\end{problem}
